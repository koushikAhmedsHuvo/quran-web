{
  "name": "quran-backend",
  "version": "1.0.0",
  "description": "Hono backend for Quran API powered by Bun — data stored as JSON files",
  "scripts": {
    "start": "bun run src/index.ts",
    "dev": "bun --hot run src/index.ts",
    "download": "bun run scripts/migrate.ts"
  },
  "dependencies": {
    "@hono/swagger-ui": "^0.6.1",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
